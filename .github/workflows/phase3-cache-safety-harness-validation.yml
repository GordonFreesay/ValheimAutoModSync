name: Phase 3 Cache Safety Harness Validation

on:
  push:
    branches:
      - dev/2.6
    paths:
      - "Source/ValheimAutoModSync.Server.cs"
      - "test-phase3-cache-safety-live.ps1"
      - ".github/workflows/phase3-cache-safety-harness-validation.yml"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  validate-harness:
    runs-on: windows-latest
    timeout-minutes: 10
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Validate source documentation
        shell: powershell
        run: powershell -NoProfile -ExecutionPolicy Bypass -File ".\\verify-source-docs.ps1"

      - name: Validate version consistency
        shell: powershell
        run: powershell -NoProfile -ExecutionPolicy Bypass -File ".\\verify-version.ps1"

      - name: Parse Phase 3 cache-safety harness
        shell: powershell
        run: |
          $tokens = $null
          $errors = $null
          [System.Management.Automation.Language.Parser]::ParseFile((Resolve-Path ".\\test-phase3-cache-safety-live.ps1"), [ref]$tokens, [ref]$errors) | Out-Null
          if ($errors.Count -gt 0) {
            $errors | ForEach-Object { Write-Error $_.Message }
            exit 1
          }
          Write-Host "PowerShell syntax check passed."
